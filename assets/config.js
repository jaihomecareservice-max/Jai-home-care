/* ============================================================
   Site configuration — the few things worth changing without
   touching any other file.
   ============================================================ */

/* Phone number in international form, no +, no spaces.
   Used for both the wa.me links and tel: links. */
export const PHONE = '919528560383';

/* Shown on buttons and in copy */
export const PHONE_DISPLAY = '+91 95285 60383';

/* ------------------------------------------------------------
   ENQUIRY LOGGING — set ENQUIRY_ENDPOINT to a Formspree URL
   to keep a copy of every enquiry. Leave '' for WhatsApp only.
------------------------------------------------------------ */
export const ENQUIRY_ENDPOINT = '';
